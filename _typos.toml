[default]
# Use typos:ignore-next-line to ignore the next line in the codebase
extend-ignore-re = [
  "(#|//)\\s*typos:ignore-next-line\\n.*",
  # Deliberately malformed SQL and identifiers in fixtures
  "\\busres\\b",
  "\\bSELECTbad\\b",
]

[files]
extend-exclude = []
